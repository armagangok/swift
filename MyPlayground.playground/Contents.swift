import UIKit


/// immutable variables cannot be assigned while run time but can be assgined while compile time.
/// let operand is used to declare an immutable variable.
let pi = 3.14


/// mutable variables can be assigned while run time.
/// var operand is used to declare an mutable variable.
var number = 3
